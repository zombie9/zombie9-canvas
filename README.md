# node-static-template
A node (npm) template for static sites

Features:
- scss with autoprefixer & stylelint
- posthtml
- eslint (extends Standard)
- webpack
- babel
- onchange
- browsersync

Useage:

- build: `npm run build`
- serve: `npm run watch`
- or `npm run start` to combine both
